<Window x:Class="Dusza_Fogadas.BetStatisticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dusza_Fogadas"
        mc:Ignorable="d"
        Title="BetStatisticsWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <local:Header
            Title="Game Statistics"/>

        <ComboBox x:Name="cbGame"
                  Grid.Row="1" SelectionChanged="cbGame_SelectionChanged"/>
        <DataGrid x:Name="dgStatistics"
                CanUserAddRows="False"
                IsReadOnly="True"
                Grid.Row="2"/>
    </Grid>
</Window>
